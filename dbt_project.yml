name: 'my_dbt_projects'
version: '1.0.0'
config-version: 2

profile: 'my_dbt_projects'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

# Snowflake best practice
quoting:
  identifier: false

models:
  my_dbt_projects:

    bronze:
      +schema: BRONZELAYER
      +materialized: view

    silver:
      +schema: SILVERLAYER
      +materialized: table

    gold:
      +schema: GOLDLAYER

      dimensions:
        +materialized: table

      facts:
        +materialized: table
        
      semantic:
        +materialized: view

